<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.delivery.mapper.OwnerMapper">


    <insert id="save">
        insert into owner(id, email, password, name, phone_number)
        values (#{id}, #{email}, #{password}, #{name}, #{phoneNumber});
    </insert>

    <select id="existsById" resultType="boolean">
        select exists(select id from owner where id = #{id});
    </select>

    <select id="getCountById" resultType="long">
        select count(id)
        from owner;
    </select>

    <select id="findMemberById" resultType="MemberDto">
        select id, password, name, email, phone_number
        from owner
        where id = #{id};
    </select>
</mapper>