<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.delivery.mapper.UserMapper">

    <insert id="save">
        INSERT INTO USER(id, email, password, name, phone_number, level)
        VALUES (#{id}, #{email}, #{password}, #{name}, #{phoneNumber}, #{level});
    </insert>

    <select id="hasUserById" resultType="boolean">
        SELECT exists(SELECT id FROM user WHERE id = #{id});
    </select>

    <select id="getCountById" resultType="long">
        SELECT count(id)
        FROM user;
    </select>

    <select id="findUserById" resultType="UserDto">
        SELECT id, password, name, email, phone_number, level
        FROM user
        WHERE id = #{id};
    </select>
</mapper>