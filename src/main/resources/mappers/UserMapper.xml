<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.delivery.mapper.UserMapper">


    <select id="idExists" resultType="boolean">
        SELECT EXISTS(SELECT id FROM USER LIMIT1 WHERE id = #{id})
    </select>


    <insert id="save" parameterType="com.flab.delivery.dto.SignUpDto">
        INSERT INTO USER(id, email, password, name, phone_number, type)
        VALUES (#{id}, #{email}, #{password}, #{name}, #{phoneNumber}, #{type})
    </insert>

    <select id="findById" resultType="com.flab.delivery.dto.UserDto">
        SELECT *
        FROM USER
        WHERE id = #{id}
    </select>


</mapper>