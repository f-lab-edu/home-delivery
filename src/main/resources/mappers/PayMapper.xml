<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.delivery.mapper.PayMapper">

    <insert id="save">
        INSERT INTO PAY(orders_id, type, status)
        VALUES (#{orderId}, #{type}, #{status});
    </insert>
</mapper>