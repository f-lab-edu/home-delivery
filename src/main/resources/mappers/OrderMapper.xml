<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.delivery.mapper.OrderMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="order.id">
        INSERT INTO ORDERS(user_id, order_price, status, delivery_address, history)
        VALUES (#{userId}, #{order.totalPrice}, #{order.orderStatus},
                #{order.deliveryAddress},
                #{order.orderHistoryDto , typeHandler = com.flab.delivery.mapper.handler.JsonTypeHandler})
    </insert>

    <update id="changeStatus">
        UPDATE ORDERS
        SET status = #{orderStatus}
        WHERE id = #{orderId};
    </update>

    <select id="findPageIds" resultType="Long">
        SELECT id
        FROM ORDERS
        WHERE id &gt;= #{startId}
          AND user_id = #{userId}
    </select>

    <select id="findAllByPageIds" resultType="OrderSimpleResponseDto">
        SELECT status,
               order_price,
               created_at,
               json_extract(history, '$.storeId')              AS store_id,
               json_extract(history, '$.menuCount')            AS menu_count,
               json_extract(history, '$.menuList[0].menuName') AS menu_name
        FROM ORDERS
        WHERE id IN
        <foreach collection="list" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
        ORDER BY created_at DESC LIMIT 10;
    </select>

</mapper>